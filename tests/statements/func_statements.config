def f(x):
    x + 1
    return x


def clip(x, a, b):
    if x > b:
        return b
    elif x < a:
        return a

    return x


def define_in_if(x):
    if x < 0:
        value = -x
    else:
        value = x

    return value


def multiple_wildcard_unpack(x):
    y, _, _ = x
    return y
